services:
  gitea:
    image: gitea/gitea:1.22
    volumes:
      - gitea-data:/data
      - ./docker/gitea-custom-hooks:/data/gitea/custom/hooks:ro
    environment:
      GITEA__database__DB_TYPE: sqlite3
      GITEA__server__ROOT_URL: http://localhost:3000/git/
      GITEA__server__HTTP_PORT: "3000"
      GITEA__server__CUSTOM_HOOKS_PATH: /data/gitea/custom/hooks
      GITEA__security__INSTALL_LOCK: "true"
      GITEA__service__DISABLE_REGISTRATION: "true"

  gitea-bootstrap:
    image: gitea/gitea:1.22
    depends_on:
      - gitea
    volumes:
      - gitea-data:/data
      - bootstrap-secrets:/bootstrap
      - ./docker/gitea-bootstrap.sh:/bootstrap/gitea-bootstrap.sh:ro
    environment:
      GITEA_ADMIN_USERNAME: ${GITEA_ADMIN_USERNAME:-eslroot}
      GITEA_ADMIN_PASSWORD: ${GITEA_ADMIN_PASSWORD}
      GITEA_ADMIN_TOKEN_FILE: ${GITEA_ADMIN_TOKEN_FILE:-/bootstrap/gitea-admin-token}
    entrypoint: ["/bin/sh", "/bootstrap/gitea-bootstrap.sh"]

  api:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
      args:
        NPM_PROXY: ${NPM_PROXY:-}
    environment:
      PORT: "3000"
      DATABASE_PATH: /data/esl.db
      GITEA_URL: http://gitea:3000
      GITEA_ADMIN_TOKEN_FILE: ${GITEA_ADMIN_TOKEN_FILE:-/bootstrap/gitea-admin-token}
      GITEA_ADMIN_USERNAME: ${GITEA_ADMIN_USERNAME:-eslroot}
      GITEA_ADMIN_PASSWORD: ${GITEA_ADMIN_PASSWORD}
      GITEA_REPO_OWNER: ${GITEA_REPO_OWNER:-esl-skills}
      ESL_BOOTSTRAP_ADMIN_TOKEN: ${ESL_BOOTSTRAP_ADMIN_TOKEN:-bootstrap-token}
    volumes:
      - api-data:/data
      - bootstrap-secrets:/bootstrap:ro
    depends_on:
      gitea-bootstrap:
        condition: service_completed_successfully

  server:
    image: nginx:1.27-alpine
    ports:
      - "3000:80"
    volumes:
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      api:
        condition: service_started
      gitea:
        condition: service_started

volumes:
  gitea-data:
  bootstrap-secrets:
  api-data:
