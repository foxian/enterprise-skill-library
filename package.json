{
  "name": "enterprise-skill-library-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @esl/core && npm run build --workspace @esl/server && npm run build --workspace @esl/cli && npm run build --workspace @esl/web",
    "deploy:web": "npm run build --workspace @esl/web && docker compose up -d server",
    "smoke:git-backend-maintenance-entry": "node scripts/check-git-backend-maintenance-entry.mjs",
    "reset:dev": "node scripts/reset-dev-env.mjs",
    "reload:dev": "npm run build && docker compose build api && docker compose up -d api server",
    "test": "npm run build --workspace @esl/core && vitest run && npm test --workspace @esl/web",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^20.11.0",
    "toad-cache": "3.3.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
