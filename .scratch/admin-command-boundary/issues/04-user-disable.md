# 04 — Admin can disable users

**What to build:** `esl admin user disable <username>` disables a user and revokes their ESL-managed access.

**Blocked by:** 03 — Admin can create users and issue login tokens

**Status:** resolved

- [x] A platform administrator can disable a user from ESL.
- [x] A disabled user can no longer continue using ESL-managed access.
